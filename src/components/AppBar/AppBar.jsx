import Navigation from "../Navigation/Navigation";
import { Header } from './AppBar.styles';

export const AppBar = () => {
    return (
        <Header>
            <Navigation />
         </Header>
     )
}